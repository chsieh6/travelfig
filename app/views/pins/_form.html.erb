<%= simple_form_for(@pin) do |f| %>
  <%= f.error_notification %>

  <%= f.input :descriptions, as: :text, input_html: { row: "5"} %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
